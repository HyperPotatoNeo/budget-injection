[project]
name = "budget-injection-env"
version = "0.1.0"
description = "Budget injection wrapper for verifiers environments"
requires-python = ">=3.10"
dependencies = ["verifiers"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
